# Autenia Viral Shorts — herramienta interna.
#
# El fork venía con la pila de recortar vídeos largos (torch, ultralytics,
# mediapipe, faster-whisper, scenedetect, yt-dlp: ~3 GB). Este producto no
# recorta vídeo ajeno: escribe un guion, lo narra y lo compone. Nada de eso
# hace falta, y quitarlo es lo que permite que quepa en un VPS pequeño.

# Modelos, voz y publicación
httpx==0.28.1
python-dotenv==1.2.2

# Almacén de contenidos (autenia/). SQLite vía aiosqlite — deliberadamente
# separado de la base Postgres del upstream comercial.
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0

# Tarjetas tipográficas y medición de texto en el render
Pillow==12.2.0

# ffmpeg y ffprobe NO son paquetes de Python: instálalos en el sistema
# (`apt install ffmpeg` / `winget install ffmpeg`). Dockerfile.bot ya lo hace.
